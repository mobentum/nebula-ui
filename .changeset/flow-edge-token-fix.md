---
"@mobentum/nebula-flow": patch
---

Fix flow canvas edges rendering invisible by pointing the `--xy-*` theme variables at the `--color-nb-*` token scale instead of the undefined `--nb-*` variables.
