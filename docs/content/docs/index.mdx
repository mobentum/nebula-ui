---
title: Nebula UI
description: A Base UI + Tailwind CSS component library for building modern web applications.
---

## Getting Started

Nebula UI is a component library built on [Base UI](https://base-ui.com) and Tailwind CSS v4, designed to be accessible, composable, and easy to style.

### Installation

```bash
npm install @mobentum/nebula-ui
```

With other package managers:

```bash
pnpm add @mobentum/nebula-ui
yarn add @mobentum/nebula-ui
bun add @mobentum/nebula-ui
```

### Tailwind CSS setup

Add the Nebula UI styles to your global CSS. Requires Tailwind CSS v4.

```css
@import "tailwindcss";
@import "@mobentum/nebula-ui/css/tailwind.css";
@import "@mobentum/nebula-ui/css/default-theme.css";

/* Include Nebula UI components for utility generation */
@source "../node_modules/@mobentum/nebula-ui/dist";
```

`tailwind.css` maps the theme tokens to `nb-*` utilities, and `default-theme.css` provides the default light/dark color variables. See [Theme](/theme/tokens) to customize.

### Usage

```tsx
import { Button, DialogClose, DialogContent, DialogDescription, DialogFooter, DialogHeader, DialogRoot, DialogTitle, DialogTrigger } from '@mobentum/nebula-ui';

function App() {
  return (
    <DialogRoot>
      <DialogTrigger render={<Button>Open Dialog</Button>} />
      <DialogContent>
        <DialogHeader>
          <DialogTitle>Welcome</DialogTitle>
          <DialogDescription>This is a Nebula UI dialog.</DialogDescription>
        </DialogHeader>
        <DialogFooter>
          <DialogClose render={<Button variant="outline">Cancel</Button>} />
          <Button>Save</Button>
        </DialogFooter>
      </DialogContent>
    </DialogRoot>
  );
}
```

## Components

Nebula UI provides a comprehensive set of components organized by category.
