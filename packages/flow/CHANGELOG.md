# @mobentum/nebula-flow

## 1.0.1

### Patch Changes

- 8effccc: Fix flow canvas edges rendering invisible by pointing the `--xy-*` theme variables at the `--color-nb-*` token scale instead of the undefined `--nb-*` variables.

## 1.0.0

### Major Changes

- c6b433c: Rename packages from `@nebula/*` to `@mobentum/nebula-*`. Breaking change — update all imports and dependency names.

### Patch Changes

- Updated dependencies [c6b433c]
  - @mobentum/nebula-ui@1.0.0
